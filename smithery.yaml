name: splitfast-mcp
displayName: SplitFast — Group Expense MCP
description: Minimal expense splitter MCP (create_group, add_expense, balances).

startCommand:
  type: http
  commandFunction: |
    (config) => ({
      command: "python",
      args: ["server.py"],
      env: {}
    })

# Tell Smithery which port your MCP HTTP server listens on
http:
  port: 8080

configSchema:
  type: object
  properties: {}
  additionalProperties: false

